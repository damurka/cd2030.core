% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-builder.R
\name{report_presets}
\alias{report_presets}
\title{The standard reports}
\usage{
report_presets(lang = "en", group = get_selected_group())
}
\arguments{
\item{lang}{The language of the report's text: \code{"en"}, \code{"fr"} or \code{"pt"}.}

\item{group}{The indicator group (\code{"rmncah"} or \code{"vaccine"}); each has its own list.}
}
\value{
A named list of reports in the order of the analysis, each \code{list(name, description, kind, design, cover, blocks)} for a document (\code{kind = "document"}), or \code{list(name, description, kind = "deck", design, cover, slides)}
for a slide deck (see \code{\link[=export_deck]{export_deck()}}). \code{\link[=report_project_blocks]{report_project_blocks()}} gives the blocks of either.
}
\description{
One per analysis section (data quality, adjustment, denominators, national coverage, inequality, mortality,
service utilization, health system, private sector), then the synthesis chartbook and the sub-national one-pager: the
Countdown report templates rebuilt as blocks, which the builder opens as editable copies. The one-pager is about one
region: its blocks use the report's region (\code{region = "@report"}). It is a slide deck of one dense slide.
}
