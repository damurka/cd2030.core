% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp_server.R
\name{cd2030_mcp_server}
\alias{cd2030_mcp_server}
\title{Run the cd2030.core MCP Server}
\usage{
cd2030_mcp_server(max_sessions = 3L, max_rows = 200L)
}
\arguments{
\item{max_sessions}{Maximum number of \code{.rds} caches to keep open at once.
Loading beyond this evicts the least-recently-used cache. Default \code{3}.}

\item{max_rows}{Maximum number of rows any single tool call returns before
truncating (with a notice telling the caller how to narrow the request).
Default \code{200}.}
}
\value{
Does not return; blocks the R process serving the MCP protocol
over stdio until the client disconnects.
}
\description{
Starts a \href{https://modelcontextprotocol.io}{Model Context Protocol} server,
over stdio, that lets an LLM client (e.g. Claude Desktop or Claude Code)
query already-processed Countdown 2030 datasets: data quality scores,
coverage, inequality, mortality, service utilization, health system
metrics, private-sector splits, mapping data, generated reports, and
coverage charts.

The server is read-only: no tool can create or modify a \code{.rds} cache, or
change any analysis parameter (denominators, k-factors, survey estimates,
...) on one. A cache must already be fully processed -- e.g. via the
Countdown 2030 Shiny app -- before pointing this server at it; tools that
need parameters which aren't set on the cache will surface
\code{CacheConnection}'s own error explaining what's missing.

Several caches can be open at once, addressed by their file path (see
the \code{load_cache} tool); the oldest is evicted once \code{max_sessions} are open.
}
\details{
This requires the \code{mcptools} and \code{ellmer} packages, which are optional
(\code{Suggests}) dependencies of cd2030.core -- install them with
\code{install.packages(c("mcptools", "ellmer"))} before calling this function.

To use with an MCP client such as Claude Desktop or Claude Code, point its
configuration at:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Rscript -e "cd2030.core::cd2030_mcp_server()"
}\if{html}{\out{</div>}}
}
