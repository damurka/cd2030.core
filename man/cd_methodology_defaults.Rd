% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methodology-defaults.R
\name{cd_methodology_defaults}
\alias{cd_methodology_defaults}
\title{The Constants of the Countdown Methodology}
\usage{
cd_methodology_defaults(group = c("all", "rmncah", "vaccine"))
}
\arguments{
\item{group}{Which entries to return: \code{"all"} (the default), or those that
apply to the \code{"rmncah"} or the \code{"vaccine"} indicator group (entries for
\code{"both"} are included in each).}
}
\value{
A tibble with one row per constant and the columns:
\itemize{
\item \code{id}: a stable snake_case identifier.
\item \code{step}: one of \code{"data_quality"}, \code{"adjustment"}, \code{"denominators"},
\code{"coverage"}, \code{"subnational"}, \code{"mortality"}.
\item \code{group}: \code{"both"}, \code{"rmncah"} or \code{"vaccine"}.
\item \code{value}: a list column; each element is a number, a string or a vector.
\item \code{unit}: for example \code{"\%"}, \code{"x MAD"}, \code{"ratio"}, \code{"proportion"}, or \code{""}.
\item \code{label}: a short English label.
\item \code{note}: one English sentence on how the app uses it.
\item \code{source}: the file of the package (within \verb{R/}) that uses it.
}
}
\description{
Lists the constants the package's methodology uses (cutoffs, thresholds,
default rates and targets) with the values the code runs with, so the
documentation can be generated from them.
}
\examples{
cd_methodology_defaults()
cd_methodology_defaults("vaccine")

}
