% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0_data_quality_checks.R
\name{check_country_recognized}
\alias{check_country_recognized}
\title{Check the Admin sheet's single country value is actually a recognized country name, pre-merge}
\usage{
check_country_recognized(parts, admin_sheet_name)
}
\arguments{
\item{parts}{Named list of cleaned per-sheet tibbles.}

\item{admin_sheet_name}{Name of the admin sheet within \code{parts}.}
}
\description{
\code{check_single_country()} (Tier A, above) only catches more than one distinct value -- the
genuinely-one-value-but-unrecognized case is \code{match_country()}'s job, which normally only runs
at Finish (\code{merge_and_standardize()}'s \code{new_countdown()} call) under the deferred-merge design.
Left uncaught pre-merge, a user could walk the entire wizard only to hit a hard abort on the very
last step -- this surfaces the same problem at Data Quality instead, non-abortingly, same as
every other check here.
}
\details{
A deliberately NOT-yet-replicated counterpart: "every indicator the selected group needs is
present" (\code{check_required_columns_exist()}, \code{utils.R}) still only runs at Finish. Its check is
against columns \code{standardize_data()} computes/renames during the merge itself (reporting-rate
\verb{_rr} columns, \code{instdeliveries} -> \code{ideliv}) -- checking for those names against the RAW,
pre-standardize sheets produces false positives (confirmed live: a real, known-good file flagged
as "missing" columns it actually has, just not yet under their final names) that would incorrectly
block a user with no real problem. A correct pre-merge version would need to replicate
\code{standardize_data()}'s own rename/compute logic just to know what to look for -- a real gap
(Finish can still abort on this), left for follow-up work rather than shipped half-right.
}
