% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0_data_quality_checks.R
\name{check_admin_pairing_consistency}
\alias{check_admin_pairing_consistency}
\title{Check a district's admin-1 pairing is stable within the Admin sheet itself}
\usage{
check_admin_pairing_consistency(admin_data)
}
\arguments{
\item{admin_data}{The raw Admin sheet (\code{parts[[admin_sheet_name]]}, pre-rename -- still has
\code{first_admin_level}, not \code{adminlevel_1}).}
}
\description{
Pre-merge half of the old (still-existing, still used post-merge by \code{new_countdown()}'s Tier B)
\code{check_district_consistency()} -- this half only ever needed the Admin sheet: a district's
\code{adminlevel_1} pairing can only vary across the merged data if the Admin sheet itself lists that
district more than once with different values, so checking the raw sheet directly loses no
signal.
}
