% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-countdown.R
\name{report_block_kinds}
\alias{report_block_kinds}
\title{The charts and tables a report can contain}
\usage{
report_block_kinds(group = get_selected_group())
}
\arguments{
\item{group}{The indicator group; kinds that only make sense for one group (\code{groups}) are left out of the other.}
}
\value{
A named list; each entry has \code{type} (\code{"chart"} or \code{"table"}), \code{group}, \code{label}, \code{indicators} (\code{"analysis"},
a character vector, or \code{NULL} when the kind has no indicator), \code{levels}, \code{variants} (named character vector or
\code{NULL}), \code{year} (whether a year is chosen), \code{regional} (whether it is drawn for one region) and \code{groups} (the
indicator groups it is for).
}
\description{
What each kind draws and which settings it takes. The builder offers these; \code{\link[=render_report_block]{render_report_block()}} draws them.
}
\details{
The kinds are listed in the order of the analysis (their \code{group}: data quality, adjustment, denominators, national,
sub-national, region, mortality, service utilization, health system). The standard report files add their own kinds
(see the top of the standard reports section).
}
