% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chart-options.R
\name{cd_chart_catalog}
\alias{cd_chart_catalog}
\title{The charts that have saved options}
\usage{
cd_chart_catalog(cache)
}
\arguments{
\item{cache}{A \link{CacheConnection}.}
}
\value{
A tibble with \code{id}, \code{target} (\code{"screen"}, \code{"report"} or \code{"dataset"}), \code{group}, \code{n_options} and \code{options} (a
one-line summary).
}
\description{
A table of every chart with options saved in a dataset's cache: which chart (its \code{\link[=cd_chart_id]{cd_chart_id()}}, or for the screen the
chart's place in the app), whether they are for the screen or for reports, how many options are set, and the group it
belongs to (the first two parts of its id, e.g. \code{coverage_filtered.national}). Use it to see which chart ids a report will
pick options up for.
}
