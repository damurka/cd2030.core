% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.completeness.R
\name{plot.cd_completeness_summary}
\alias{plot.cd_completeness_summary}
\title{Plot Missing Summary}
\usage{
\method{plot}{cd_completeness_summary}(
  x,
  plot_type = c("heat_map", "trend"),
  indicator = NULL,
  title = NULL,
  x_axis = NULL,
  y_axis = NULL,
  legend = NULL,
  ...,
  options = NULL
)
}
\arguments{
\item{x}{A \code{cd_completeness_summary} object, as returned by
\code{\link[=calculate_completeness_summary]{calculate_completeness_summary()}}.}

\item{plot_type}{Either \code{"heat_map"} (missingness by unit, for one or all
indicators) or \code{"trend"} (completeness over time for one indicator).
Default is \code{"heat_map"}.}

\item{indicator}{Optional. One of the supported indicators (\code{'opv1'}, \code{'penta3'}, etc.)
to visualize in the plot. If \code{NULL} all indicators will be shown.}

\item{title}{Optional plot title. Defaults to a title based on \code{plot_type},
\code{indicator} and region.}

\item{x_axis, y_axis}{Optional x- and y-axis titles. Default is \code{NULL}.}

\item{legend}{Optional legend title. Default is \code{NULL}.}

\item{...}{Reserved for future use.}

\item{options}{(Optional) A \code{\link[=cd_chart_options]{cd_chart_options()}} object: text, legend, fonts and sizes a user changed. Applied last, so it wins over the
arguments above; the plot's own defaults are used for whatever it does not set. Any chart option can also be given by name in \code{...}.}
}
\value{
A \code{ggplot} or \code{plotly} object depending on the selection type.
}
\description{
This method visualizes missing results for immunization indicators
}
\examples{
\dontrun{
# Region-level summary
plot(missing_data, indicator = "penta3")
}

}
