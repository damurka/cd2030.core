% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0_data_quality_checks.R
\name{check_admin_columns}
\alias{check_admin_columns}
\title{Check the Admin_data sheet has the columns every later step assumes exist}
\usage{
check_admin_columns(admin_data)
}
\arguments{
\item{admin_data}{The raw, cleaned Admin_data sheet (one \code{read_and_clean_sheet()} call's
output, before \code{merge_data()}).}
}
\description{
\code{first_admin_level}/\code{country} are only ever consumed via \code{any_of()} downstream (see
\code{read_and_clean_sheet()}'s own \code{required_columns}, used only for \code{drop_na()}, and
\code{standardize_data()}'s \code{rename(adminlevel_1 = any_of("first_admin_level"))}), so a sheet
missing either column silently proceeds today with \code{adminlevel_1}/\code{country} never created,
surfacing later as a confusing, unrelated error far from the actual cause. \code{district} isn't
checked here -- it's already covered by \code{read_and_clean_sheet()}'s own key-column check.
}
