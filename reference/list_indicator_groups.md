# List available indicator group names

Returns all **current** groups (built-ins + any overrides).

## Usage

``` r
list_indicator_groups()
```

## Value

A sorted character vector of group names.

## Examples

``` r
list_indicator_groups()
#> [1] "rmncah"  "vaccine"
```
